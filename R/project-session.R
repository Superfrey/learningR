10
source(here::here("R/package-loading.R"))

# Basic of R --------------------------------------------------------------

weight_kilos <- 100
weight_kilos

c("a", "b", "c")
c(TRUE, FALSE)
c(1, 2, 4)
factor(c("high", "medium", "low"))

# Overview
head(CO2, 10)

# Variable names
colnames(CO2)

# Datastructure
str(CO2)

# Despritive overview
summary(CO2)

colnames(CO2)



